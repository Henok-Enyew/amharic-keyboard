[project]
name = "amharic-ibus-fedora"
version = "1.0.0"
description = "Amharic phonetic IBus engine for Fedora"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["engine/tests"]
pythonpath = ["engine"]
